bootJar.enabled = true
jar.enabled = false

group = 'com.hanghae.presentation'
version = '1.0-SNAPSHOT'

dependencies {
    implementation project(':application')
    implementation "org.springframework.boot:spring-boot-starter-web"
}