spring:
  profiles:
    active: application
  application:
    name: cinema
  datasource:
    url: jdbc:mysql://localhost:33306/cinema
    username: user
    password: 1q2w3e4r
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true
logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql:
              BasicBinder: TRACE
